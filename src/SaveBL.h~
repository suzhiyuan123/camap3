#ifndef _SAVEBL_H
#define _SAVEBL_H

#include <fstream>
#include <string>
#include <map>
#include <iostream>

#include "BlockList.h"

class SaveBL{
	public:
		SaveBL();
		~SaveBL();
		static int savebl(char *file,BlockList bl);
		static BlockList readbl(char *file);
};

#endif
